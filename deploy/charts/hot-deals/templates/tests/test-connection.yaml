apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hot-deals.fullname" . }}-test-connection"
  labels:
{{ include "hot-deals.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "hot-deals.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
