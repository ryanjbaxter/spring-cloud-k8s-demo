apiVersion: v1
kind: Service
metadata:
  name: {{ include "hot-deals.fullname" . }}
  labels:
{{ include "hot-deals.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "hot-deals.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
