apiVersion: v1
kind: Secret
metadata:
  name: {{ include "gateway.fullname" . }}-spring
  labels:
{{ include "gateway.labels" . | indent 4 }}
type: Opaque
data:
{{- if .Values.secret }}
{{ .Values.secret | toYaml | indent 2 }}
{{- end }}