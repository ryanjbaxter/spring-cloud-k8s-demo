apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fashion-bestseller.fullname" . }}-test-connection"
  labels:
{{ include "fashion-bestseller.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "fashion-bestseller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
