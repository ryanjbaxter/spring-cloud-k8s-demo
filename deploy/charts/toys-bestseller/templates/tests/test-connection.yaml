apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "toys-bestseller.fullname" . }}-test-connection"
  labels:
{{ include "toys-bestseller.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "toys-bestseller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
